rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas abiertas para desarrollo y primer uso
    // TODO: Restringir en producción con Firebase Auth
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
